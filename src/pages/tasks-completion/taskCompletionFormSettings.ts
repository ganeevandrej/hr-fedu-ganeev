import { CompleteTaskRequestModel } from '@models/tasks';

const defaultValues: CompleteTaskRequestModel = {
	workType: '',
	workPrice: null,
	completeDate: null,
	comment: '',
};

export { defaultValues };
